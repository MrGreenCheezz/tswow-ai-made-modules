/**
 * Ручные списки для распределения камней по типам брони и ролям.
 *
 * Имена сравниваются БЕЗ РЕГИСТРА с ruRU-именем спелла (fallback enGB; на этом
 * клиенте enGB-колонки в DBC пустые — реальные имена лежат в ruRU, см.
 * tswow-gotchas). Не нашёлся в списке — работает автоэвристика:
 *   - хил: эффекты HEAL/PERIODIC_HEAL и т.п.
 *   - танк: таунты (ATTACK_ME / MOD_TAUNT)
 *   - энх-шаман: удары оружием (WEAPON_DAMAGE и т.п.) и weapon-imbue (54)
 * Эти списки — точечные поправки поверх эвристик.
 */

/** Шаман: способности, которые считаем "совершенствованием" → кожа. */
export const ENHANCEMENT_NAMES: string[] = [
    // авто-эвристика уже ловит удары оружием и имбьюи; сюда добавлять
    // исключения вида "щит молний", если решим что это энх
];

/** Способности, которые принудительно считаем танковыми (роль 2). */
export const TANK_NAMES: string[] = [
    "оборонительная стойка",
    "defensive stance",
    "блок щитом",
    "shield block",
    "глухая оборона",
    "shield wall",
    "праведная ярость",
    "righteous fury",
    "ледяное присутствие",
    "frost presence",
    "последний оплот",
    "last stand",
    "укрепление",
    "fortification",
];

/** Способности, которые принудительно считаем лекарскими (роль 1). */
export const HEAL_NAMES: string[] = [];

/** Прямой оверрайд брони по id спелла: 1 ткань, 2 кожа, 4 кольчуга, 8 латы (битовая маска). */
export const ARMOR_OVERRIDES: { [spell: number]: number } = {};

/**
 * Прямой оверрайд роли по id спелла: 0 дпс, 1 хил, 2 танк.
 * Сюда попадают лечащие спеллы, у которых на КАСТЕ нет HEAL-эффекта (лечит
 * прок или призванный тотем) — авто-эвристика их не видит. Для шамана роль
 * "хил" дополнительно означает ткань (рестор → ткань по плану распределения).
 */
export const ROLE_OVERRIDES: { [spell: number]: number } = {
    // Оружие Живой земли (лечащий имбью рестора; эвристика сочла энхом → кожа)
    51730: 1, 51988: 1, 51991: 1, 51992: 1, 51993: 1, 51994: 1,
    // Тотем исцеляющего потока (лечит тотем, на касте только summon)
    5394: 1, 6375: 1, 6377: 1, 10462: 1, 10463: 1, 25567: 1,
    58755: 1, 58756: 1, 58757: 1,
};
